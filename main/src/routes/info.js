export default function Info() {
    return (
        <div>Welcome to my reactjs based music tracker, this is an experiment to push react a bit to see if it can handle a lot of rapid updates and still be capable of taking in user input through a complex ui.</div>
    );
  }